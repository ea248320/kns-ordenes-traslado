// La app se sirve bajo un subdirectorio en GitHub Pages
// (/kns-ordenes-traslado/), así que las rutas a archivos públicos deben
// llevar el BASE_URL de Vite en vez de una ruta absoluta desde la raíz.
export const LOGO_URL = `${import.meta.env.BASE_URL}logo.jpg`
